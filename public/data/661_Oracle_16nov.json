[
    {
        "id": 1879,
        "title": "Code Bracket Validator",
        "problem_statement": "<h1>Code Bracket Validator</h1>\r\n\r\n<h2>Problem Description</h2>\r\n<p>You are given an array of strings, where each string contains only the characters '(', ')', '[', ']', '{', and '}'. For each string, determine if the string is valid.</p>\r\n\r\n<p>A string is valid if:</p>\r\n<ul>\r\n    <li>Every opening bracket has a matching closing bracket of the same type.</li>\r\n    <li>Brackets close in the correct order.</li>\r\n</ul>\r\n\r\n<p>The function <code>isValidBrackets</code> will take one input:</p>\r\n<p><code>string queries[q]: the string queries</code></p>\r\n\r\n<p>The function should return an array of strings, where each element is \"YES\" if the corresponding input string is valid, or \"NO\" otherwise.</p>\r\n\r\n<h2>Examples</h2>\r\n<h3>Example 1:</h3>\r\n<pre><code class='language-text'>Input:\r\nq = 4\r\nqueries = [\"{[]}\", \"({)}\", \"{}\", \"{][}\"]\r\nOutput:\r\nYES\r\nNO\r\nYES\r\nNO</code></pre>\r\n<p><strong>Explanation:</strong> Analysis of each bracket sequence is as follows:</p>\r\n<ol>\r\n    <li><code>\"{[]}\"</code> &rarr; Brackets are correctly matched and nested.</li>\r\n    <li><code>\"({)}\"` &rarr; Incorrect closing: ')' closes before '{' is matched.</li>\r\n    <li><code>\"{}\"</code> &rarr; Brackets are properly matched and in correct order.</li>\r\n    <li><code>\"{][}\"</code> &rarr; Starts with closing bracket ']' without a match.</li>\r\n</ol>\r\n<p>Hence, the answer is [\"YES\", \"NO\", \"YES\", \"NO\"].</p>\r\n\r\n<h3>Example 2:</h3>\r\n<pre><code class='language-text'>Input:\r\nq = 4\r\nqueries = [\"((()))\", \"[{()}]\", \"(()\", \"({[})\"]\r\nOutput:\r\nYES\r\nYES\r\nNO\r\nNO</code></pre>\r\n<p><strong>Explanation:</strong> Analysis of each bracket sequence is as follows:</p>\r\n<ol>\r\n    <li><code>\"((()))\"</code> &rarr; All brackets are properly matched and nested.</li>\r\n    <li><code>\"[{()}]\"</code> &rarr; All brackets are properly matched and nested.</li>\r\n    <li><code>\"(()\"</code> &rarr; Missing closing bracket for '('.</li>\r\n    <li><code>\"({[})\"` &rarr; Incorrect closing: ')' closes before '[' is matched.</li>\r\n</ol>\r\n\r\n<h2>Constraints</h2>\r\n<ul>\r\n    <li><code>1 &lt;= q &lt;= 2 * 10^5</code></li>\r\n    <li><code>1 &lt;= length of queries[i] &lt;= 2 * 10^5</code></li>\r\n    <li>It is guaranteed that the sum of the lengths of strings over all the queries does not exceed <code>2 * 10^5</code>.</li>\r\n</ul>",
        "google_doc_link": null,
        "solution_code": null,
        "pregiven_code": "p\n#include <iostream>\n#include <vector>\n#include <string>\n#include <stack>\n\nclass Solution {\npublic:\n    std::vector<std::string> isValidBrackets(const std::vector<std::string>& queries) {\n        std::vector<std::string> results;\n        results.reserve(queries.size());\n\n        // Core logic to validate each bracket string goes here.\n        // For each string in `queries`, determine if it's valid.\n        // If valid, push \"YES\" to `results`. Otherwise, push \"NO\".\n        \n        return results;\n    }\n};\n\nint main() {\n    std::ios_base::sync_with_stdio(false);\n    std::cin.tie(NULL);\n\n    int q;\n    std::cin >> q;\n\n    std::vector<std::string> queries(q);\n    for (int i = 0; i < q; ++i) {\n        std::cin >> queries[i];\n    }\n\n    Solution sol;\n    std::vector<std::string> results = sol.isValidBrackets(queries);\n\n    for (const auto& res : results) {\n        std::cout << res << \"\\n\";\n    }\n\n    return 0;\n}",
        "input_test_case": null,
        "output_test_case": null,
        "company_name": "Oracle_16nov",
        "company_id": 661
    },
    {
        "id": 1880,
        "title": "Roll the String",
        "problem_statement": "<h1>Roll the String</h1>\r\n\r\n<h2>Problem Description</h2>\r\n<p>A single roll operation increments each character by one in a circular manner within the English alphabet (a-z). For example, 'a' becomes 'b', 'b' becomes 'c', and 'z' becomes 'a'.</p>\r\n<p>Given a string <code>s</code> and an array of integers <code>roll</code>, perform a roll operation on the first <code>roll[i]</code> characters of <code>s</code> for each element in the array.</p>\r\n\r\n<h2>Function Description</h2>\r\n<p>Complete the function <code>rollTheString</code> in the editor with the following parameter(s):</p>\r\n<ul>\r\n    <li><code>string s</code>: the string to operate on</li>\r\n    <li><code>int roll[n]</code>: an array where each element <code>roll[i]</code> specifies the number of characters from the beginning of the string to roll. <code>n</code> is the size of the array.</li>\r\n</ul>\r\n<p><strong>Returns:</strong></p>\r\n<ul>\r\n    <li><code>string</code>: the resulting string after all roll operations have been performed</li>\r\n</ul>\r\n<p>The C function signature provided in the editor is:</p>\r\n<pre><code class='language-c'>char* rollTheString(char* s, int roll_count, int* roll)</code></pre>\r\n\r\n<h2>Examples</h2>\r\n<h3>Example 1:</h3>\r\n<pre><code class='language-text'>Input: s = \"abz\", roll = [3, 2, 1]\r\nOutput: \"dda\"</code></pre>\r\n<p><strong>Explanation:</strong></p>\r\n<ol>\r\n    <li><code>roll[0] = 3</code>: Roll all three characters. 'abz' becomes 'bca'.</li>\r\n    <li><code>roll[1] = 2</code>: Roll the first two characters. 'bca' becomes 'cda'.</li>\r\n    <li><code>roll[2] = 1</code>: Roll the first character. 'cda' becomes 'dda'.</li>\r\n</ol>\r\n<p>The final value of <code>s</code> is 'dda'.</p>\r\n\r\n<h3>Example 2:</h3>\r\n<pre><code class='language-text'>Input: s = \"abz\", roll = [3]\r\nOutput: \"bca\"</code></pre>\r\n<p><strong>Explanation:</strong> Roll forward the first 3 characters in the substring <code>s[0] - s[2]</code>, so 'abz' becomes 'bca'.</p>\r\n\r\n<h2>Constraints</h2>\r\n<ul>\r\n    <li>Each character in <code>s</code> is in the range <code>ascii('a')-ascii('z')</code>.</li>\r\n    <li><code>1 <= length of s <= 10^5</code></li>\r\n    <li><code>1 <= n <= 10^5</code></li>\r\n    <li><code>1 <= roll[i] <= length of s</code>, where <code>0 <= i < n</code>.</li>\r\n</ul>\r\n\r\n<h2>Input Format for Custom Testing</h2>\r\n<p>Sample Input 0:</p>\r\n<pre><code class='language-text'>abz\r\n1\r\n3</code></pre>\r\n<p>Sample Output 0:</p>\r\n<pre><code class='language-text'>bca</code></pre>",
        "google_doc_link": null,
        "solution_code": null,
        "pregiven_code": "p\n#include <iostream>\n#include <vector>\n#include <string>\n#include <numeric>\n\nclass Solution {\npublic:\n    /**\n     * @param s The string to operate on.\n     * @param roll An array where each element roll[i] specifies the number of characters\n     *             from the beginning of the string to roll.\n     * @return The resulting string after all roll operations have been performed.\n     */\n    std::string rollTheString(std::string s, std::vector<int>& roll) {\n        // TODO: Implement the solution logic here\n        return \"\";\n    }\n};\n\nint main() {\n    std::ios_base::sync_with_stdio(false);\n    std::cin.tie(NULL);\n\n    std::string s;\n    std::cin >> s;\n\n    int n;\n    std::cin >> n;\n\n    std::vector<int> roll(n);\n    for (int i = 0; i < n; ++i) {\n        std::cin >> roll[i];\n    }\n\n    Solution solution;\n    std::string result = solution.rollTheString(s, roll);\n\n    std::cout << result << std::endl;\n\n    return 0;\n}",
        "input_test_case": null,
        "output_test_case": null,
        "company_name": "Oracle_16nov",
        "company_id": 661
    }
]